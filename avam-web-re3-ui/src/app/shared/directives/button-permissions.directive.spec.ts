import { ButtonPermissionsDirective } from './button-permissions.directive';

describe('ButtonPermissionsDirective', () => {
    it('should create an instance', () => {
        const directive = new ButtonPermissionsDirective();
        expect(directive).toBeTruthy();
    });
});
