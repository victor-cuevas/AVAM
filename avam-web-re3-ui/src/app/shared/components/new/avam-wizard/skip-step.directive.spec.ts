import { SkipStepDirective } from './skip-step.directive';

describe('SkipStepDirective', () => {
  it('should create an instance', () => {
    const directive = new SkipStepDirective();
    expect(directive).toBeTruthy();
  });
});
