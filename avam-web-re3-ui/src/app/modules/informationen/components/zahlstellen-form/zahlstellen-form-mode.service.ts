import { Injectable } from '@angular/core';
import { FormMode } from '@app/shared';

@Injectable()
export class ZahlstellenFormModeService extends FormMode {
    constructor() {
        super();
    }
}
