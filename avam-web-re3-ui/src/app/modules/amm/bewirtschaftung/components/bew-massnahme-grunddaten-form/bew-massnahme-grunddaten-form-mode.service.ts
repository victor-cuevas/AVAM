import { Injectable } from '@angular/core';
import { FormMode } from '@app/shared';

@Injectable()
export class BewMassnahmeGrunddatenFormModeService extends FormMode {
    constructor() {
        super();
    }
}
